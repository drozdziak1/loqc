# action.yml
name: 'Tilt build action'
description: 'This will run `tilt ci` in a container'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
